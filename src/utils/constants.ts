export enum DesignoRoutes {
  Home = "/",
  WebDesign = "/web",
  AppDesign = "/app",
  GraphicDesign = "/graphic",
  AboutUs = "/about",
  Locations = "/locations",
  Contact = "/contact",
}

export const MAX_WIDTH_CONTENT = 1200;
